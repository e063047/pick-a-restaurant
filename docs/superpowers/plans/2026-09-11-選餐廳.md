# 選餐廳程式 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建一個純前端靜態網頁，讓使用者依距離/葷素/用餐時間篩選附近餐廳，隨機抽出最多 10 間放上轉盤，轉出一間後提供 Google 地圖連結。

**Architecture:** 單一靜態網站（`index.html` + `style.css` + `js/*.js`，用原生 ES modules，無 build 流程）。資料來源是 OpenStreetMap 的 Overpass API（免費、瀏覽器可直接跨來源查詢，已驗證回應含 `Access-Control-Allow-Origin: *`），定位失敗時用 Nominatim 做地址轉座標（同樣支援 CORS）。營業時間判斷用開源函式庫 `opening_hours.js`（CDN 載入）。部署到 GitHub Pages。

**Tech Stack:** 原生 HTML/CSS/JavaScript（ES modules）、Overpass API、Nominatim、opening_hours.js（CDN，鎖定版本 3.5.0）、GitHub Pages。

**Spec:** `docs/superpowers/specs/2026-09-11-選餐廳-design.md`

## Global Constraints

- 不使用 Google Places/Maps API 做搜尋或評價查詢；Google 地圖只用來組出跳轉連結。
- 不使用前端框架、不使用任何 build 工具；全部原生 HTML/CSS/JS。
- 不需要帳號系統、不存個資、不使用資料庫；完全匿名單次使用。
- 不使用自動化測試框架（規模小、單頁應用）；每個任務用手動步驟驗證（開瀏覽器主控台執行指定程式碼，核對印出結果）。
- 距離選項固定為 500m / 1km / 3km / 5km。
- 葷素篩選「素食」時只保留明確標註 `diet:vegetarian=yes` 或 `diet:vegan=yes` 的店。
- 沒有標註 `opening_hours` 的店**予以保留**，標示為「營業時間未知」；只有明確標註且判斷為休息中的店才被排除。
- 篩選後最多隨機抽 10 間；不到 10 間就用實際數量，一間都沒有就顯示提示訊息。
- **本機測試方式**：專案用 ES modules，不能用 `file://` 直接開啟 `index.html`（瀏覽器會擋 module 的跨來源限制）。每次要手動測試時，先在專案根目錄執行 `python3 -m http.server 8000`，再用瀏覽器開啟 `http://localhost:8000/`。

---

### Task 1: 專案骨架（index.html / style.css / js/app.js 骨架）

**Files:**
- Create: `index.html`
- Create: `style.css`
- Create: `js/app.js`

**Interfaces:**
- Produces：完整的 DOM 結構與元素 id（`distance-select`、`diet-select`、`date-input`、`time-input`、`address-input`、`search-btn`、`status-message`、`form-section`、`wheel-section`、`wheel-canvas`、`wheel-pointer`、`spin-btn`、`restart-btn`、`result-section`、`result-name`、`result-address`、`result-phone`、`result-open-status`、`result-diet`、`maps-link`、`restart-btn-2`），後續所有任務都會用到這些 id。

- [ ] **Step 1: 建立 index.html**

```html
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>選餐廳轉盤</title>
<link rel="stylesheet" href="style.css">
</head>
<body>
<div id="app">
  <h1>今天吃什麼？</h1>

  <section id="form-section">
    <label for="distance-select">搜尋距離</label>
    <select id="distance-select">
      <option value="500">500 公尺</option>
      <option value="1000" selected>1 公里</option>
      <option value="3000">3 公里</option>
      <option value="5000">5 公里</option>
    </select>

    <label for="diet-select">飲食偏好</label>
    <select id="diet-select">
      <option value="all" selected>全部</option>
      <option value="vegetarian">素食</option>
      <option value="meat">葷食</option>
    </select>

    <label for="date-input">用餐日期</label>
    <input type="date" id="date-input">

    <label for="time-input">用餐時間</label>
    <input type="time" id="time-input">

    <input type="text" id="address-input" placeholder="輸入地址（定位失敗時使用）" hidden>

    <button id="search-btn">找餐廳</button>
    <p id="status-message"></p>
  </section>

  <section id="wheel-section" hidden>
    <div id="wheel-wrapper">
      <div id="wheel-pointer"></div>
      <canvas id="wheel-canvas" width="320" height="320"></canvas>
    </div>
    <button id="spin-btn">轉動轉盤</button>
    <button id="restart-btn">重新搜尋</button>
  </section>

  <section id="result-section" hidden>
    <h2 id="result-name"></h2>
    <p>地址：<span id="result-address"></span></p>
    <p>電話：<span id="result-phone"></span></p>
    <p>營業狀態：<span id="result-open-status"></span></p>
    <p>類型：<span id="result-diet"></span></p>
    <a id="maps-link" target="_blank" rel="noopener">在 Google 地圖開啟</a>
    <button id="restart-btn-2">再抽一次</button>
  </section>
</div>

<script src="https://cdn.jsdelivr.net/npm/opening_hours@3.5.0/opening_hours.js"></script>
<script type="module" src="js/app.js"></script>
</body>
</html>
```

- [ ] **Step 2: 建立 style.css（基礎版面，Task 10 會再補響應式細節）**

```css
* { box-sizing: border-box; }

body {
  font-family: -apple-system, "Noto Sans TC", sans-serif;
  max-width: 480px;
  margin: 0 auto;
  padding: 16px;
  color: #1d1d1d;
  background: #fafafa;
}

h1 { font-size: 1.4rem; text-align: center; }

label { display: block; margin-top: 12px; font-weight: bold; }

select, input, button {
  width: 100%;
  padding: 10px;
  margin-top: 4px;
  font-size: 1rem;
  border-radius: 6px;
  border: 1px solid #ccc;
}

button {
  margin-top: 16px;
  background: #d62828;
  color: white;
  border: none;
  cursor: pointer;
}

button:disabled { opacity: 0.5; cursor: not-allowed; }

#status-message { color: #b00020; text-align: center; }

#wheel-wrapper {
  position: relative;
  width: 320px;
  margin: 24px auto;
}

#wheel-pointer {
  position: absolute;
  top: -4px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 14px solid transparent;
  border-right: 14px solid transparent;
  border-top: 22px solid #d62828;
  z-index: 2;
}

#wheel-canvas { display: block; }

#result-section { text-align: center; }

#maps-link {
  display: block;
  padding: 10px;
  margin-top: 12px;
  background: #1a73e8;
  color: white;
  text-decoration: none;
  border-radius: 6px;
}
```

- [ ] **Step 3: 建立 js/app.js（先只做日期/時間預設值，驗證整體載入正常）**

```js
// js/app.js
function pad(n) {
  return String(n).padStart(2, '0');
}

const now = new Date();
document.getElementById('date-input').value =
  `${now.getFullYear()}-${pad(now.getMonth() + 1)}-${pad(now.getDate())}`;
document.getElementById('time-input').value = `${pad(now.getHours())}:${pad(now.getMinutes())}`;

console.log('app loaded');
```

- [ ] **Step 4: 手動驗證**

在專案根目錄執行：

```bash
python3 -m http.server 8000
```

用瀏覽器開啟 `http://localhost:8000/`，打開 DevTools 主控台，確認：
1. 主控台印出 `app loaded`，沒有任何紅色錯誤訊息。
2. 「用餐日期」欄位已自動填入今天日期，「用餐時間」欄位已自動填入目前時間。
3. 畫面上只看得到表單區塊（距離、飲食偏好、日期、時間、找餐廳按鈕），轉盤與結果區塊應該是隱藏的。

- [ ] **Step 5: Commit**

```bash
git add index.html style.css js/app.js
git commit -m "$(cat <<'EOF'
新增專案骨架：表單、轉盤、結果區塊的 HTML/CSS 結構

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 2: 定位模組（js/geo.js）

**Files:**
- Create: `js/geo.js`

**Interfaces:**
- Consumes：無（不依賴其他任務的程式碼）
- Produces：
  - `getUserLocation(): Promise<{lat: number, lon: number}>` — 成功時 resolve 座標，使用者拒絕授權或瀏覽器不支援時 reject 一個帶有中文訊息的 `Error`
  - `geocodeAddress(address: string): Promise<{lat: number, lon: number}>` — 查無結果或服務失敗時 reject 一個帶有中文訊息的 `Error`

- [ ] **Step 1: 建立 js/geo.js**

```js
// js/geo.js

export function getUserLocation() {
  return new Promise((resolve, reject) => {
    if (!navigator.geolocation) {
      reject(new Error('此瀏覽器不支援定位功能'));
      return;
    }
    navigator.geolocation.getCurrentPosition(
      (position) => {
        resolve({ lat: position.coords.latitude, lon: position.coords.longitude });
      },
      (error) => {
        reject(new Error('無法取得您的位置：' + error.message));
      },
      { enableHighAccuracy: false, timeout: 10000, maximumAge: 60000 }
    );
  });
}

export async function geocodeAddress(address) {
  const url =
    'https://nominatim.openstreetmap.org/search?format=json&limit=1&q=' +
    encodeURIComponent(address);
  const response = await fetch(url, {
    headers: { 'Accept-Language': 'zh-TW' },
  });
  if (!response.ok) {
    throw new Error('地址查詢服務暫時無法使用');
  }
  const results = await response.json();
  if (results.length === 0) {
    throw new Error('找不到這個地址，請試試更完整的地址');
  }
  return { lat: parseFloat(results[0].lat), lon: parseFloat(results[0].lon) };
}
```

- [ ] **Step 2: 手動驗證 geocodeAddress**

啟動本機伺服器後開啟 `http://localhost:8000/`，在 DevTools 主控台執行：

```js
import('./js/geo.js').then((m) =>
  m.geocodeAddress('台北101').then(console.log).catch(console.error)
);
```

預期輸出一個物件，`lat` 約 `25.03`、`lon` 約 `121.56`（誤差在小數點後兩位內都算正常）。

再測試查無結果的情況：

```js
import('./js/geo.js').then((m) =>
  m.geocodeAddress('這是一個不存在的地址亂打一通xyz123').then(console.log).catch(console.error)
);
```

預期在主控台看到 `Error: 找不到這個地址，請試試更完整的地址`。

- [ ] **Step 3: 手動驗證 getUserLocation**

在主控台執行：

```js
import('./js/geo.js').then((m) => m.getUserLocation().then(console.log).catch(console.error));
```

瀏覽器會跳出定位權限請求，點「允許」，預期印出 `{lat: ..., lon: ...}`，數值應接近你目前所在位置。

- [ ] **Step 4: Commit**

```bash
git add js/geo.js
git commit -m "$(cat <<'EOF'
新增定位模組：瀏覽器定位與地址轉座標（Nominatim）

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 3: Overpass 查詢模組（js/overpass.js）

**Files:**
- Create: `js/overpass.js`

**Interfaces:**
- Consumes：無
- Produces：
  - `queryRestaurants(lat: number, lon: number, radiusMeters: number): Promise<Restaurant[]>`
  - `Restaurant` 物件形狀（後續所有任務都沿用這個形狀）：
    ```
    {
      id: number,
      name: string,
      lat: number,
      lon: number,
      cuisine: string | null,
      dietVegetarian: boolean,
      dietVegan: boolean,
      openingHours: string | null,
      phone: string | null,
      address: string | null
    }
    ```
  - 沒有 `name` 標籤的店會被過濾掉（無法放上轉盤）。
  - 兩個 Overpass 節點都查詢失敗時，reject 一個帶中文訊息的 `Error`。

- [ ] **Step 1: 建立 js/overpass.js**

```js
// js/overpass.js

const ENDPOINTS = [
  'https://overpass-api.de/api/interpreter',
  'https://overpass.kumi.systems/api/interpreter',
];

function buildQuery(lat, lon, radiusMeters) {
  return `[out:json][timeout:25];
(
  node["amenity"~"^(restaurant|fast_food|cafe)$"](around:${radiusMeters},${lat},${lon});
);
out body;`;
}

function parseElement(el) {
  const tags = el.tags || {};
  if (!tags.name) return null;
  const address =
    tags['addr:full'] ||
    [tags['addr:city'], tags['addr:street'], tags['addr:housenumber']]
      .filter(Boolean)
      .join('');
  return {
    id: el.id,
    name: tags.name,
    lat: el.lat,
    lon: el.lon,
    cuisine: tags.cuisine || null,
    dietVegetarian: tags['diet:vegetarian'] === 'yes',
    dietVegan: tags['diet:vegan'] === 'yes',
    openingHours: tags.opening_hours || null,
    phone: tags.phone || tags['contact:phone'] || null,
    address: address || null,
  };
}

async function queryEndpoint(endpoint, query) {
  const response = await fetch(endpoint, {
    method: 'POST',
    body: 'data=' + encodeURIComponent(query),
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
  });
  if (!response.ok) {
    throw new Error(`Overpass 查詢失敗：HTTP ${response.status}`);
  }
  return response.json();
}

export async function queryRestaurants(lat, lon, radiusMeters) {
  const query = buildQuery(lat, lon, radiusMeters);
  let lastError = null;
  for (const endpoint of ENDPOINTS) {
    try {
      const data = await queryEndpoint(endpoint, query);
      return (data.elements || []).map(parseElement).filter((r) => r !== null);
    } catch (err) {
      lastError = err;
    }
  }
  throw new Error('查詢餐廳資料失敗，請稍後再試：' + (lastError ? lastError.message : ''));
}
```

- [ ] **Step 2: 手動驗證**

在主控台執行（座標是台北101，範圍 1000 公尺）：

```js
import('./js/overpass.js').then((m) =>
  m.queryRestaurants(25.0338, 121.5645, 1000).then((list) => {
    console.log('數量：', list.length);
    console.log('第一筆：', list[0]);
  })
);
```

預期：`數量` 大於 0；`第一筆` 物件含有 `id`、`name`、`lat`、`lon` 等欄位，且 `name` 不是空字串。

- [ ] **Step 3: Commit**

```bash
git add js/overpass.js
git commit -m "$(cat <<'EOF'
新增 Overpass 查詢模組：以 OSM 免費資料搜尋附近餐廳，含備援節點

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 4: 營業時間判斷模組（js/openingHours.js）

**Files:**
- Create: `js/openingHours.js`

**Interfaces:**
- Consumes：全域變數 `window.opening_hours`（由 `index.html` 中的 CDN `<script>` 標籤提供，Task 1 已加入）
- Produces：
  - `isOpenAt(openingHoursString: string | null, date: Date): boolean | null` — `openingHoursString` 為空、或解析失敗時回傳 `null`（代表未知）；否則回傳該時間點是否營業。

- [ ] **Step 1: 建立 js/openingHours.js**

```js
// js/openingHours.js
// 依賴全域的 window.opening_hours（由 index.html 載入的 opening_hours.js 函式庫提供）

export function isOpenAt(openingHoursString, date) {
  if (!openingHoursString) return null;
  try {
    const oh = new window.opening_hours(openingHoursString);
    return oh.getState(date);
  } catch (err) {
    return null;
  }
}
```

- [ ] **Step 2: 手動驗證**

啟動本機伺服器並開啟頁面（確保 CDN 的 opening_hours.js 有載入成功，DevTools Network 分頁可看到該請求回傳 200）。在主控台執行：

```js
import('./js/openingHours.js').then((m) => {
  const now = new Date();
  const day = now.getDay(); // 0=週日 ... 6=週六
  const monday = new Date(now);
  monday.setDate(now.getDate() - day + 1);
  monday.setHours(10, 0, 0, 0);
  const sunday = new Date(now);
  sunday.setDate(now.getDate() - day);
  sunday.setHours(10, 0, 0, 0);

  console.log('週一早上10點，Mo-Fr 09:00-18:00 應該營業中 ->', m.isOpenAt('Mo-Fr 09:00-18:00', monday));
  console.log('週日早上10點，Mo-Fr 09:00-18:00 應該休息中 ->', m.isOpenAt('Mo-Fr 09:00-18:00', sunday));
  console.log('沒有標註 ->', m.isOpenAt(null, now));
  console.log('格式錯誤的標註 ->', m.isOpenAt('not a real format', now));
});
```

預期：第一行 `true`，第二行 `false`，第三、四行都是 `null`。

- [ ] **Step 3: Commit**

```bash
git add js/openingHours.js
git commit -m "$(cat <<'EOF'
新增營業時間判斷模組，包裝 opening_hours.js 並將解析失敗視為未知

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 5: 篩選模組（js/filters.js）

**Files:**
- Create: `js/filters.js`

**Interfaces:**
- Consumes：`isOpenAt` from `js/openingHours.js`（Task 4）；`Restaurant` 物件形狀（Task 3）
- Produces：
  - `filterByDiet(restaurants: Restaurant[], dietOption: 'all' | 'vegetarian' | 'meat'): Restaurant[]`
  - `filterByOpeningHours(restaurants: Restaurant[], dateTime: Date): (Restaurant & {openStatus: 'open' | 'closed' | 'unknown'})[]` — 排除 `openStatus === 'closed'` 的店，其餘（含 `unknown`）保留並附加 `openStatus` 欄位

- [ ] **Step 1: 建立 js/filters.js**

```js
// js/filters.js
import { isOpenAt } from './openingHours.js';

export function filterByDiet(restaurants, dietOption) {
  if (dietOption === 'all') return restaurants;
  if (dietOption === 'vegetarian') {
    return restaurants.filter((r) => r.dietVegetarian || r.dietVegan);
  }
  if (dietOption === 'meat') {
    return restaurants.filter((r) => !(r.dietVegetarian || r.dietVegan));
  }
  throw new Error('未知的葷素選項：' + dietOption);
}

export function filterByOpeningHours(restaurants, dateTime) {
  return restaurants
    .map((r) => {
      const state = isOpenAt(r.openingHours, dateTime);
      const openStatus = state === null ? 'unknown' : state ? 'open' : 'closed';
      return { ...r, openStatus };
    })
    .filter((r) => r.openStatus !== 'closed');
}
```

- [ ] **Step 2: 手動驗證**

在主控台執行：

```js
import('./js/filters.js').then((m) => {
  const sample = [
    { name: '葷食店A', dietVegetarian: false, dietVegan: false, openingHours: null },
    { name: '素食店B', dietVegetarian: true, dietVegan: false, openingHours: null },
    { name: '全素店C', dietVegetarian: false, dietVegan: true, openingHours: null },
    { name: '休息中店D', dietVegetarian: false, dietVegan: false, openingHours: 'Mo-Fr 09:00-10:00' },
  ];

  console.log('素食篩選 ->', m.filterByDiet(sample, 'vegetarian').map((r) => r.name));
  // 預期 ['素食店B', '全素店C']

  console.log('葷食篩選 ->', m.filterByDiet(sample, 'meat').map((r) => r.name));
  // 預期 ['葷食店A', '休息中店D']（沒標素食的都算葷食）

  const noon = new Date();
  noon.setHours(23, 0, 0, 0); // 特意選一個「Mo-Fr 09:00-10:00」不會營業的時間點
  const openFiltered = m.filterByOpeningHours(sample, noon);
  console.log('營業時間篩選（應排除休息中店D） ->', openFiltered.map((r) => `${r.name}:${r.openStatus}`));
  // 預期看到 葷食店A:unknown、素食店B:unknown、全素店C:unknown，「休息中店D」應該被排除
});
```

- [ ] **Step 3: Commit**

```bash
git add js/filters.js
git commit -m "$(cat <<'EOF'
新增葷素與營業時間篩選模組

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 6: 隨機抽樣模組（js/sample.js）

**Files:**
- Create: `js/sample.js`

**Interfaces:**
- Consumes：無
- Produces：`sampleRandom(array: T[], n: number): T[]` — 從陣列中不重複隨機抽出最多 `n` 個元素，`array` 長度小於 `n` 時回傳全部（打亂順序）

- [ ] **Step 1: 建立 js/sample.js**

```js
// js/sample.js

export function sampleRandom(array, n) {
  const copy = array.slice();
  for (let i = copy.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [copy[i], copy[j]] = [copy[j], copy[i]];
  }
  return copy.slice(0, Math.min(n, copy.length));
}
```

- [ ] **Step 2: 手動驗證**

在主控台執行：

```js
import('./js/sample.js').then((m) => {
  const source = Array.from({ length: 12 }, (_, i) => i + 1);
  const picked = m.sampleRandom(source, 10);
  console.log('抽出數量（預期10）->', picked.length);
  console.log('是否有重複（預期10，代表沒有重複）->', new Set(picked).size);

  const small = m.sampleRandom([1, 2, 3], 10);
  console.log('來源只有3筆時抽出數量（預期3）->', small.length);
});
```

重複執行幾次第一段，確認每次抽出的內容/順序不完全相同（驗證有隨機性）。

- [ ] **Step 3: Commit**

```bash
git add js/sample.js
git commit -m "$(cat <<'EOF'
新增隨機抽樣模組，用 Fisher-Yates 洗牌抽出最多 N 筆候選餐廳

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 7: Google 地圖連結模組（js/mapsLink.js）

**Files:**
- Create: `js/mapsLink.js`

**Interfaces:**
- Consumes：無
- Produces：`buildGoogleMapsLink(name: string, lat: number, lon: number): string`

- [ ] **Step 1: 建立 js/mapsLink.js**

```js
// js/mapsLink.js

export function buildGoogleMapsLink(name, lat, lon) {
  const query = encodeURIComponent(`${name} ${lat},${lon}`);
  return `https://www.google.com/maps/search/?api=1&query=${query}`;
}
```

- [ ] **Step 2: 手動驗證**

在主控台執行：

```js
import('./js/mapsLink.js').then((m) => {
  const url = m.buildGoogleMapsLink('鼎泰豐', 25.0338, 121.5645);
  console.log(url);
});
```

把印出的網址複製到新分頁打開，確認 Google 地圖正確開啟並顯示接近台北101的位置、搜尋框內容包含「鼎泰豐」。

- [ ] **Step 3: Commit**

```bash
git add js/mapsLink.js
git commit -m "$(cat <<'EOF'
新增 Google 地圖連結產生模組，純網址組合不呼叫 Google API

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 8: 轉盤模組（js/wheel.js）

**Files:**
- Create: `js/wheel.js`

**Interfaces:**
- Consumes：`Restaurant` 物件的 `name` 欄位（其餘欄位不使用）；`canvas` DOM 元素（`#wheel-canvas`，Task 1 已建立，寬高 320x320）
- Produces：
  - `pickWinnerIndex(n: number): number` — 回傳 `0` 到 `n-1` 之間均勻分布的隨機整數
  - `class Wheel { constructor(canvas, restaurants); draw(); spin(onFinish: (winner: Restaurant) => void); }`
  - 指針固定在畫布正上方（12 點鐘方向）；`spin()` 動畫結束後會把轉到指針位置的那個扇形對應的餐廳物件傳給 `onFinish`

- [ ] **Step 1: 建立 js/wheel.js**

```js
// js/wheel.js

const COLORS = [
  '#e63946', '#f1a208', '#2a9d8f', '#457b9d', '#ff6b6b',
  '#6a4c93', '#43aa8b', '#f3722c', '#577590', '#f9844a',
];

export function pickWinnerIndex(n) {
  return Math.floor(Math.random() * n);
}

export class Wheel {
  constructor(canvas, restaurants) {
    this.canvas = canvas;
    this.ctx = canvas.getContext('2d');
    this.restaurants = restaurants;
    this.rotation = 0;
  }

  draw() {
    const { ctx, canvas, restaurants, rotation } = this;
    const cx = canvas.width / 2;
    const cy = canvas.height / 2;
    const radius = Math.min(cx, cy) - 10;
    const sliceAngle = (2 * Math.PI) / restaurants.length;

    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.save();
    ctx.translate(cx, cy);
    ctx.rotate(rotation);

    restaurants.forEach((r, i) => {
      const start = i * sliceAngle;
      const end = start + sliceAngle;

      ctx.beginPath();
      ctx.moveTo(0, 0);
      ctx.arc(0, 0, radius, start, end);
      ctx.closePath();
      ctx.fillStyle = COLORS[i % COLORS.length];
      ctx.fill();

      ctx.save();
      ctx.rotate(start + sliceAngle / 2);
      ctx.textAlign = 'right';
      ctx.fillStyle = '#fff';
      ctx.font = '14px sans-serif';
      let label = r.name;
      if (label.length > 8) label = label.slice(0, 7) + '…';
      ctx.fillText(label, radius - 8, 4);
      ctx.restore();
    });

    ctx.restore();
  }

  spin(onFinish) {
    const n = this.restaurants.length;
    const winnerIndex = pickWinnerIndex(n);
    const sliceAngle = (2 * Math.PI) / n;
    const winnerCenter = winnerIndex * sliceAngle + sliceAngle / 2;
    // 指針固定在 -90 度（正上方）；多轉 5 圈製造轉動感
    const targetRotation = -Math.PI / 2 - winnerCenter + Math.PI * 2 * 5;
    const startRotation = this.rotation;
    const duration = 4000;
    const startTime = performance.now();
    const easeOutCubic = (t) => 1 - Math.pow(1 - t, 3);

    const step = (now) => {
      const elapsed = now - startTime;
      const t = Math.min(elapsed / duration, 1);
      const eased = easeOutCubic(t);
      this.rotation = startRotation + (targetRotation - startRotation) * eased;
      this.draw();
      if (t < 1) {
        requestAnimationFrame(step);
      } else {
        onFinish(this.restaurants[winnerIndex]);
      }
    };
    requestAnimationFrame(step);
  }
}
```

- [ ] **Step 2: 手動驗證 pickWinnerIndex 的均勻性**

在主控台執行：

```js
import('./js/wheel.js').then((m) => {
  const counts = {};
  for (let i = 0; i < 2000; i++) {
    const idx = m.pickWinnerIndex(5);
    counts[idx] = (counts[idx] || 0) + 1;
  }
  console.log(counts);
});
```

預期：`counts` 印出 `0` 到 `4` 五個 key，每個 key 的數值大致落在 `400` 上下（誤差在合理範圍內，不會有某個 key 是 0 或遠高於其他）。

- [ ] **Step 3: 手動驗證轉盤繪製與旋轉**

在主控台執行（先把轉盤區塊顯示出來）：

```js
document.getElementById('wheel-section').hidden = false;
import('./js/wheel.js').then((m) => {
  const canvas = document.getElementById('wheel-canvas');
  const sample = ['餐廳A', '餐廳B', '餐廳C', '餐廳D', '餐廳E'].map((name) => ({ name }));
  const wheel = new m.Wheel(canvas, sample);
  wheel.draw();
  window.__testWheel = wheel; // 暫存，方便下一步呼叫 spin
});
```

確認畫面上出現一個 5 色扇形的轉盤，每個扇形上有餐廳名稱文字，正上方有一個紅色三角形指針。接著執行：

```js
window.__testWheel.spin((winner) => console.log('抽中：', winner.name));
```

確認轉盤會旋轉並在約 4 秒後平滑減速停止，主控台印出的「抽中」餐廳名稱，其扇形應該正好對齊正上方的指針。多執行幾次，確認每次停下的位置不同（隨機性）。

測試完後刷新頁面即可清除 `window.__testWheel`。

- [ ] **Step 4: Commit**

```bash
git add js/wheel.js
git commit -m "$(cat <<'EOF'
新增轉盤模組：Canvas 繪製扇形與帶減速動畫的隨機旋轉

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 9: 主程式整合（js/app.js）

**Files:**
- Modify: `js/app.js`（取代 Task 1 的骨架內容）

**Interfaces:**
- Consumes：
  - `getUserLocation`, `geocodeAddress` from `js/geo.js`（Task 2）
  - `queryRestaurants` from `js/overpass.js`（Task 3）
  - `filterByDiet`, `filterByOpeningHours` from `js/filters.js`（Task 5）
  - `sampleRandom` from `js/sample.js`（Task 6）
  - `buildGoogleMapsLink` from `js/mapsLink.js`（Task 7）
  - `Wheel` from `js/wheel.js`（Task 8）
  - Task 1 建立的所有 DOM 元素 id
- Produces：完整可操作的使用者流程（表單 → 轉盤 → 結果），供 Task 10/11 做樣式與部署

- [ ] **Step 1: 覆寫 js/app.js**

```js
// js/app.js
import { getUserLocation, geocodeAddress } from './geo.js';
import { queryRestaurants } from './overpass.js';
import { filterByDiet, filterByOpeningHours } from './filters.js';
import { sampleRandom } from './sample.js';
import { buildGoogleMapsLink } from './mapsLink.js';
import { Wheel } from './wheel.js';

function pad(n) {
  return String(n).padStart(2, '0');
}

const formSection = document.getElementById('form-section');
const wheelSection = document.getElementById('wheel-section');
const resultSection = document.getElementById('result-section');
const statusMessage = document.getElementById('status-message');
const distanceSelect = document.getElementById('distance-select');
const dietSelect = document.getElementById('diet-select');
const dateInput = document.getElementById('date-input');
const timeInput = document.getElementById('time-input');
const addressInput = document.getElementById('address-input');
const searchBtn = document.getElementById('search-btn');
const spinBtn = document.getElementById('spin-btn');
const restartBtn = document.getElementById('restart-btn');
const restartBtn2 = document.getElementById('restart-btn-2');
const wheelCanvas = document.getElementById('wheel-canvas');

const now = new Date();
dateInput.value = `${now.getFullYear()}-${pad(now.getMonth() + 1)}-${pad(now.getDate())}`;
timeInput.value = `${pad(now.getHours())}:${pad(now.getMinutes())}`;

let currentWheel = null;

function setStatus(message) {
  statusMessage.textContent = message || '';
}

function showSection(section) {
  [formSection, wheelSection, resultSection].forEach((s) => {
    s.hidden = s !== section;
  });
}

async function resolveLocation() {
  const manualAddress = addressInput.value.trim();
  if (manualAddress) {
    return geocodeAddress(manualAddress);
  }
  try {
    return await getUserLocation();
  } catch (err) {
    addressInput.hidden = false;
    throw new Error('無法自動定位，請在下方輸入地址後再試一次');
  }
}

async function handleSearch() {
  searchBtn.disabled = true;
  setStatus('查詢中，可能需要幾秒鐘...');
  try {
    const { lat, lon } = await resolveLocation();
    const radius = Number(distanceSelect.value);
    const dateTimeValue = new Date(`${dateInput.value}T${timeInput.value}:00`);

    const rawRestaurants = await queryRestaurants(lat, lon, radius);
    const dietFiltered = filterByDiet(rawRestaurants, dietSelect.value);
    const openFiltered = filterByOpeningHours(dietFiltered, dateTimeValue);

    if (openFiltered.length === 0) {
      setStatus('這範圍內沒有符合條件的餐廳，試試看擴大距離或改選「全部」');
      return;
    }

    const candidates = sampleRandom(openFiltered, 10);
    setStatus('');
    showSection(wheelSection);
    currentWheel = new Wheel(wheelCanvas, candidates);
    currentWheel.draw();
  } catch (err) {
    setStatus(err.message);
  } finally {
    searchBtn.disabled = false;
  }
}

function handleSpin() {
  spinBtn.disabled = true;
  currentWheel.spin((winner) => {
    spinBtn.disabled = false;
    showResult(winner);
  });
}

function showResult(restaurant) {
  document.getElementById('result-name').textContent = restaurant.name;
  document.getElementById('result-address').textContent = restaurant.address || '地址未知';
  document.getElementById('result-phone').textContent = restaurant.phone || '電話未知';
  const statusText = { open: '營業中', closed: '休息中', unknown: '營業時間未知' }[restaurant.openStatus];
  document.getElementById('result-open-status').textContent = statusText;
  const dietText = restaurant.dietVegan
    ? '素食（全素）'
    : restaurant.dietVegetarian
    ? '素食（蛋奶素）'
    : '一般餐廳';
  document.getElementById('result-diet').textContent = dietText;
  const link = document.getElementById('maps-link');
  link.href = buildGoogleMapsLink(restaurant.name, restaurant.lat, restaurant.lon);
  showSection(resultSection);
}

function handleRestart() {
  addressInput.value = '';
  addressInput.hidden = true;
  setStatus('');
  showSection(formSection);
}

searchBtn.addEventListener('click', handleSearch);
spinBtn.addEventListener('click', handleSpin);
restartBtn.addEventListener('click', handleRestart);
restartBtn2.addEventListener('click', handleRestart);
```

- [ ] **Step 2: 手動驗證完整流程（黃金路徑）**

啟動本機伺服器，開啟 `http://localhost:8000/`：
1. 保留預設的距離（1 公里）、飲食偏好（全部）、日期時間，點「找餐廳」。
2. 瀏覽器跳出定位授權請求，點「允許」。
3. 確認畫面出現「查詢中，可能需要幾秒鐘...」提示，接著自動切換到轉盤畫面，畫面上看得到最多 10 個扇形，每個都標著餐廳名稱。
4. 點「轉動轉盤」，確認轉盤旋轉並停止，畫面切換到結果卡片，顯示店名、地址、電話、營業狀態、類型。
5. 點「在 Google 地圖開啟」，確認開新分頁正確跳轉到 Google 地圖並顯示該店家位置。
6. 回到結果卡片點「再抽一次」，確認畫面回到表單，可以重新查詢。

- [ ] **Step 3: 手動驗證邊界情況**

- **拒絕定位**：重新整理頁面，點「找餐廳」時在瀏覽器的定位授權跳窗點「封鎖」，確認畫面出現「無法自動定位，請在下方輸入地址後再試一次」提示，且地址輸入框變為可見；輸入一個有效地址（例如「台北101」）後再點一次「找餐廳」，確認能正常查到餐廳並顯示轉盤。
- **選擇很小的距離＋素食**：把距離改成 500 公尺、飲食偏好改成「素食」，在素食餐廳較少的地區查詢，確認若結果不足 10 間會直接用實際數量顯示轉盤；若完全查無結果，確認會顯示「這範圍內沒有符合條件的餐廳...」提示而不是卡住或報錯。
- **選一個打烊時段**：把用餐時間改成凌晨 3 點左右再查詢，確認結果中不會出現明確標註「打烊」的店，但仍可能出現「營業時間未知」的店。

- [ ] **Step 4: Commit**

```bash
git add js/app.js
git commit -m "$(cat <<'EOF'
整合主流程：串接定位、查詢、篩選、抽樣、轉盤與結果顯示

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 10: 樣式與響應式調整（style.css）

**Files:**
- Modify: `style.css`

**Interfaces:**
- Consumes：Task 1/9 已存在的 DOM 結構與 id，不新增任何 id
- Produces：手機寬度（約 375px）下可正常操作、無版面跑版的頁面

- [ ] **Step 1: 更新 style.css，加入間距與手機友善的調整**

在 `style.css` 檔案最後加入：

```css
@media (max-width: 400px) {
  #wheel-wrapper {
    width: 280px;
  }
  #wheel-canvas {
    width: 280px;
    height: 280px;
  }
}

#status-message:empty {
  display: none;
}

section {
  margin-top: 8px;
}

#result-section p {
  margin: 6px 0;
}
```

因為 canvas 的實際繪圖解析度（`width`/`height` 屬性）固定為 320，若要讓它在小螢幕上「顯示」縮小為 280，需要同時調整 `js/wheel.js` 使用 CSS 尺寸與繪圖尺寸分離；但這會讓轉盤在所有裝置上都變模糊或需要額外處理高解析度螢幕的 `devicePixelRatio`，複雜度不成比例。改用更簡單的作法：把 canvas 固定寬高從 320 降到 300（同時改 `index.html` 的 `width="320" height="320"` 為 `width="300" height="300"`），這樣在 iPhone SE（375px 寬）等最窄的常見裝置上也不會超出可視範圍，不需要額外的 CSS 縮放邏輯。

- [ ] **Step 2: 修改 index.html 的 canvas 尺寸**

把 `index.html` 中的：

```html
<canvas id="wheel-canvas" width="320" height="320"></canvas>
```

改成：

```html
<canvas id="wheel-canvas" width="300" height="300"></canvas>
```

同時把 `style.css` 中 `#wheel-wrapper { width: 320px; }` 改成 `width: 300px;`，並刪除 Step 1 加入的 `@media (max-width: 400px)` 區塊（不再需要）。

- [ ] **Step 3: 手動驗證**

啟動本機伺服器，用瀏覽器 DevTools 的裝置模擬工具切換到 iPhone SE（375 x 667）或任一寬度 375〜400px 的裝置，重新測試 Task 9 Step 2 的完整流程：
1. 確認表單所有欄位都完整可見，不需要橫向捲動。
2. 確認轉盤完整顯示在畫面內，不會超出螢幕寬度。
3. 確認按鈕文字清楚可讀、可正常點擊（觸控目標夠大）。
4. 確認結果卡片的文字與「在 Google 地圖開啟」按鈕排版正常。

- [ ] **Step 4: Commit**

```bash
git add style.css index.html
git commit -m "$(cat <<'EOF'
調整轉盤尺寸與間距，確保手機寬度下正常顯示

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Jyt2A3BpERazZJUrGnRb2G
EOF
)"
```

---

### Task 11: 部署到 GitHub Pages

**Files:**
- 無程式碼變更（純部署設定）

**Interfaces:**
- Consumes：整個 repo 的現有內容
- Produces：一個公開的 `https://<GitHub 帳號>.github.io/<repo 名稱>/` 網址

> 這個任務會建立公開的 GitHub repo 並推送程式碼上去，屬於「對外可見」的操作。執行前請先跟使用者確認 GitHub 帳號、repo 名稱是否 OK，並確認要用哪個帳號登入 `gh` CLI（或改用網頁手動建立）。

- [ ] **Step 1: 確認/建立 GitHub repo**

如果已安裝並登入 `gh` CLI，可執行（把 `選餐廳` 換成你想要的 repo 名稱）：

```bash
gh repo create 選餐廳 --public --source=. --remote=origin
```

若尚未安裝 `gh` 或尚未登入，改為手動到 https://github.com/new 建立一個 public repo（名稱例如 `選餐廳`），然後在專案根目錄執行（把 `<你的帳號>` 換成實際帳號）：

```bash
git remote add origin https://github.com/<你的帳號>/選餐廳.git
```

- [ ] **Step 2: 推送程式碼**

```bash
git branch -M main
git push -u origin main
```

- [ ] **Step 3: 開啟 GitHub Pages**

到該 repo 的 GitHub 網頁 → Settings → Pages：
- Source 選擇 `Deploy from a branch`
- Branch 選擇 `main`，資料夾選擇 `/ (root)`
- 儲存後等待約 1〜2 分鐘，頁面會顯示網址，格式為 `https://<你的帳號>.github.io/選餐廳/`

- [ ] **Step 4: 手動驗證上線版本**

用手機（非電腦，測試真實定位與觸控）開啟該網址，重新跑一次 Task 9 Step 2 的完整流程（找餐廳 → 授權定位 → 轉盤 → 結果 → 點開 Google 地圖）。確認：
1. 頁面能正常載入（不是 404）。
2. Overpass/Nominatim 查詢能正常運作（代表 CORS 在正式網域下也沒問題，先前已用模擬的 `Origin` header 驗證過機制本身沒問題）。
3. 轉盤與結果流程跟本機測試時一致。

- [ ] **Step 5: 把網址記錄下來**

把最終網址貼給後續要使用這個工具的人。這一步完成後整個功能即上線可用。

---

## Self-Review 紀錄

- **spec 涵蓋度**：距離/葷素/營業時間篩選（Task 3/4/5）、轉盤與 Google 地圖連結（Task 7/8/9）、定位與手動輸入地址備援（Task 2/9）、錯誤處理與查無結果提示（Task 9）、部署（Task 11）、手動測試（各任務 Step 皆有）— spec 每一段都對應到至少一個任務。
- **型別/介面一致性**：`Restaurant` 物件形狀從 Task 3 定義後，Task 5（`openStatus` 欄位）、Task 8（`name`）、Task 9（`address`/`phone`/`lat`/`lon`/`openStatus`/`dietVegan`/`dietVegetarian`）皆沿用同一組欄位名稱，沒有改名不一致的狀況。
- **無佔位符**：所有步驟皆含完整可執行的程式碼與具體的手動驗證指令/預期輸出，沒有「之後補上」「TODO」等字樣。
